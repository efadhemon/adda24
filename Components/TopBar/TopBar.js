import React from 'react';
import { Text } from 'react-native';

const TopBar = () => {
    return (
        <div>
            <Text>This is TopBar of Adda-24</Text>
        </div>
    );
};

export default TopBar;